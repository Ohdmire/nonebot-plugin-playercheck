<!DOCTYPE html>
<html>
    <head>
        <style>
            h1 {
            color:rgb(0, 0, 0);
            text-align:center;
            }
            .list {
            display: flex;
            flex-wrap: wrap;
            border-style: solid;
            justify-content: space-between;

            }

            .gamename {
            flex-direction: row;
            border-style: solid;
            margin-top:100px;
            margin-bottom:100px;
            margin-right:50px;
            margin-left:50px;
                
                
            }

            img {
                width: 50px;
                height: 50px;
            }

        </style>
       
    </head>
    <body>
        <h1>
            群音游成分查询
        </h1>
        <div class="list">
            {% for i in allgamelists %}
                <div class="gamename"><h2>{{ i }}</h2>
                {% for key,value in playerdict.items() %}
                    {% if i in value %}
                    <div class="player"><img src="http://q1.qlogo.cn/g?b=qq&nk={{ key }}&s=640" alt=""><p>{{ key }}</p></div>
                    {% endif %}
                    {% endfor %}
                </div>
            {% endfor %}
        </div>
    </body>
</html>
